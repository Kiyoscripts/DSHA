import fs from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';
import { createHash } from 'node:crypto';
import { execFileSync } from 'node:child_process';
import { site, entries } from '../data/catalog.mjs';
import { installLink, installPage } from './install-page.mjs';

const project = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..');
const repository = path.resolve(process.env.DSHA_SOURCE_ROOT || path.resolve(project, '..'));
const output = path.join(project, 'dist');
const manifest = JSON.parse(fs.readFileSync(process.env.DSHA_RELEASE_MANIFEST || path.join(repository, 'app/build/release-manifest.json'), 'utf8'));
if (manifest.schemaVersion !== 1 || manifest.packageName !== 'com.dsh.client' || !manifest.releases.length) throw new Error('Generate the release manifest from the final APKs first');
const current = manifest.releases[0];
site.version = current.version; site.versionCode = current.versionCode;
const channelLabel = current.channel === 'stable' ? 'Official' : 'Preview';
const releasePage = `${site.repository}/releases/tag/v${site.version}`;
if (current.artifacts.length !== 2 || new Set(current.artifacts.map(a=>a.flavor)).size !== 2) throw new Error('The release manifest must include both standard and compatibility builds');
const read = p => fs.readFileSync(path.join(project, p), 'utf8');
const sha = value => createHash('sha256').update(value).digest('hex');
const escape = value => String(value).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function write(name, content) { const target = path.join(output, name); fs.mkdirSync(path.dirname(target), {recursive:true}); fs.writeFileSync(target,content); }
function copy(source, name) { const target=path.join(output,name);fs.mkdirSync(path.dirname(target),{recursive:true});fs.copyFileSync(source,target); }
function digestFile(source) { return new Promise((resolve,reject)=>{const h=createHash('sha256');fs.createReadStream(source).on('data',d=>h.update(d)).on('end',()=>resolve(h.digest('hex'))).on('error',reject);}); }
const labels={builtin:'Bundled plugin',skill:'Agent Skill',plugin:'Community plugin'};
const knownIds = new Set();
for (const e of entries) {
  if (!/^[a-z0-9][a-z0-9-]{0,79}$/.test(e.id) || knownIds.has(e.id)) throw new Error('Catalog ID is invalid or duplicated');
  knownIds.add(e.id);
  if (!labels[e.kind] || !['device','workflow'].includes(e.category)) throw new Error(`Invalid catalog type: ${e.id}`);
  if (new URL(e.source).protocol !== 'https:') throw new Error(`Source must be HTTPS: ${e.id}`);
  if (e.kind === 'plugin' && (!e.download || new URL(e.download.url).protocol !== 'https:' || !/^[a-f0-9]{64}$/.test(e.download.sha256))) throw new Error(`Community plugin is missing a pinned download URL or digest: ${e.id}`);
}
const icons={
  layout:'<rect x="3" y="4" width="18" height="16" rx="2"/><path d="M9 4v16M9 10h12"/>',
  terminal:'<rect x="3" y="4" width="18" height="16" rx="2"/><path d="m7 9 3 3-3 3m6 0h4"/>',
  bell:'<path d="M18 8a6 6 0 0 0-12 0c0 7-3 7-3 9h18c0-2-3-2-3-9M10 21h4"/>',
  layers:'<path d="m12 3 10 5-10 5L2 8Zm-9 9 9 5 9-5M3 16l9 5 9-5"/>',
  scan:'<path d="M8 3H3v5m13-5h5v5M3 16v5h5m13-5v5h-5M7 9h10M7 13h10M7 17h6"/>',
  search:'<circle cx="10" cy="10" r="6"/><path d="m15 15 6 6"/>',
  grid:'<rect x="3" y="3" width="7" height="7" rx="1"/><rect x="14" y="3" width="7" height="7" rx="1"/><rect x="3" y="14" width="7" height="7" rx="1"/><rect x="14" y="14" width="7" height="7" rx="1"/>',
  list:'<path d="M8 5h13M8 12h13M8 19h13M3 5h.1M3 12h.1M3 19h.1"/>',
  sun:'<circle cx="12" cy="12" r="4"/><path d="M12 2v2m0 16v2M2 12h2m16 0h2M5 5l1.5 1.5m11 11L19 19M5 19l1.5-1.5m11-11L19 5"/>',
  moon:'<path d="M20.5 13.5A8.5 8.5 0 0 1 10.5 3 9 9 0 1 0 20.5 13.5Z"/>'
};
const icon=name=>`<svg class="icon" viewBox="0 0 24 24" aria-hidden="true">${icons[name]||icons.layout}</svg>`;
const external=(href,text,cls='')=>`<a href="${escape(href)}"${cls?` class="${escape(cls)}"`:''} target="_blank" rel="noopener noreferrer">${text} <span aria-hidden="true">↗</span></a>`;
const list=values=>`<ul>${values.map(v=>`<li>${escape(v)}</li>`).join('')}</ul>`;
const steps=values=>`<ol>${values.map(v=>`<li>${escape(v)}</li>`).join('')}</ol>`;
const code=(value,id)=>`<div class="code-block"><code${id?` id="${id}"`:''}>${escape(value)}</code></div>${id?`<button class="button small js-only" type="button" data-copy-target="${id}">Copy command</button>`:''}`;
const routes=[];
let css,app,theme,logoFile,faviconFile,logoHash;
function page({title,description,route,active='market',body,noindex=false}) {
  if(!noindex)routes.push(route);
  const nav=[['/','market','Marketplace'],['/guide/','guide','Install guide'],['/submit/','submit','Submit a plugin'],['/download/','download','Download DSHA'],['/about/','about','About']];
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><meta name="theme-color" content="#edf3fc"><meta name="color-scheme" content="light dark"><meta name="description" content="${escape(description)}">${noindex?'<meta name="robots" content="noindex">':''}<title>${escape(title)} · DSHA</title><link rel="canonical" href="${site.origin}${route}"><meta property="og:type" content="website"><meta property="og:site_name" content="DSHA"><meta property="og:title" content="${escape(title)}"><meta property="og:description" content="${escape(description)}"><meta property="og:url" content="${site.origin}${route}"><meta property="og:locale" content="en_US"><link rel="icon" href="/favicon.ico?v=${logoHash}" sizes="any"><link rel="icon" href="/assets/${faviconFile}" type="image/png" sizes="48x48"><link rel="apple-touch-icon" href="/assets/${logoFile}" sizes="192x192"><script src="/assets/${theme}"></script><link rel="stylesheet" href="/assets/${css}"><script defer src="/assets/${app}"></script></head><body><a class="skip" href="#main">Skip to main content</a><header class="topbar"><div class="shell topbar-inner"><a class="brand" href="/" aria-label="DSHA marketplace home"><img class="brand-mark" src="/assets/${logoFile}" width="38" height="38" alt=""><span>DSHA<small>Plugins and skills · Android</small></span></a><button class="nav-toggle" type="button" data-nav-toggle aria-controls="main-navigation" aria-expanded="false">Menu</button><nav class="nav" id="main-navigation" data-navigation aria-label="Main navigation">${nav.map(([href,key,label])=>`<a href="${href}"${active===key?' aria-current="page"':''}>${label}</a>`).join('')}</nav><button class="theme-button js-only" type="button" aria-label="Switch display theme" aria-pressed="false" data-theme-toggle><span class="theme-sun">${icon('sun')}</span><span class="theme-moon">${icon('moon')}</span><span data-theme-label>Light</span></button></div></header><main class="shell" id="main" tabindex="-1">${body}</main><footer class="footer"><div class="shell footer-inner"><div>DSHA · DeepSeek Harness for Android<br><span class="small-text">Adapted for ${site.version} · dsh ${site.dsh} · arm64</span></div><div class="footer-links"><a href="/security/">Permissions and data</a><a href="/guide/#faq">FAQ</a>${external(site.repository,'GitHub')}<a href="/api/catalog.json">Catalog data</a></div></div></footer><div class="toast" role="status" aria-live="polite" data-toast hidden></div></body></html>`;
}
function card(e) {
  return `<article class="card" data-entry="${e.id}" data-kind="${e.kind}" data-category="${e.category}" data-search-text="${escape([e.name,e.packageName,e.author,e.summary,...e.tags].join(' '))}"><div class="card-top"><span class="tile-icon ${e.kind}">${icon(e.icon)}</span><span class="badge">${e.kind==='builtin'?'Bundled':e.kind==='plugin'?'Community plugin':'Downloadable skill'}</span></div><h3><a href="/plugins/${e.id}/">${escape(e.name)}</a></h3><p class="package">${escape(e.packageName)}</p><p class="card-summary">${escape(e.summary)}</p><div class="tags">${e.tags.map(t=>`<span class="tag">${escape(t)}</span>`).join('')}</div><div class="card-foot"><span>${labels[e.kind]} · ${e.kind==='skill'?'rc1.1 adaptation':'v'+e.version}</span><a href="/plugins/${e.id}/">${e.kind==='builtin'?'See how to use':'View and get'} <span aria-hidden="true">→</span></a></div></article>`;
}
function market() {
  return `<section class="market-header"><div><p class="eyebrow">DSHA MARKETPLACE</p><h1>Give the agent on your phone one more capability.</h1><p class="lede">Find bundled plugins and reusable skills. Review the requirements first, then bring them into DSHA.</p></div><a class="release-pill" href="/download/"><i class="dot"></i>${channelLabel} ${site.version} <span aria-hidden="true">↗</span></a></section><div class="market-layout"><aside class="side" aria-label="Marketplace notes"><div class="side-box"><h2>Current catalog</h2><div class="count-line"><span>Bundled plugins</span><b>${entries.filter(e=>e.kind==="builtin").length}</b></div><div class="count-line"><span>Agent Skills</span><b>${entries.filter(e=>e.kind==="skill").length}</b></div><div class="count-line"><span>Community plugins</span><b>${entries.filter(e=>e.kind==="plugin").length}</b></div><p>Manage bundled plugins in the app. Download skills into the skills directory.</p></div><div class="side-box"><h2>Start here</h2><div class="side-links"><a href="/install/">Install a plugin from a link →</a><a href="/guide/#skill">Add an Agent Skill →</a><a href="/guide/#builtin">Manage bundled plugins →</a><a href="/submit/">Submit your work →</a></div></div><div class="side-box"><h2>Runtime</h2><p>Ubuntu 24.04 · Linux arm64<br>Node.js 24 · dsh ${site.dsh}</p><a class="small-text" href="/download/">Standard / compatibility</a></div></aside><section class="market-main" aria-label="Plugin and skill catalog"><div class="search-row js-only"><label class="search-wrap">${icon('search')}<span class="sr-only">Search plugins, skills, uses, or authors</span><input type="search" maxlength="200" autocomplete="off" placeholder="Search plugins, skills, uses, or authors" data-search></label></div><div class="filters js-only"><label><span class="sr-only">Entry type</span><select class="field-select" data-kind><option value="all">All types</option><option value="builtin">Bundled plugins</option><option value="skill">Agent Skills</option><option value="plugin">Community plugins</option></select></label><label><span class="sr-only">Use category</span><select class="field-select" data-category><option value="all">All uses</option><option value="device">Device actions</option><option value="workflow">Interface and workflow</option></select></label><button class="button small" type="button" data-reset>Reset</button><div class="view-switch" role="group" aria-label="Catalog layout"><button type="button" data-view-button="grid" aria-label="Grid view" aria-pressed="true">${icon('grid')}</button><button type="button" data-view-button="list" aria-label="List view" aria-pressed="false">${icon('list')}</button></div></div><div class="result-row"><span role="status" aria-live="polite" data-results>${entries.length} entries</span><span>Checked ${site.checkedAt}</span></div><noscript><p class="notice">The full catalog is shown. Details and downloads work without JavaScript. Enable JavaScript to search and filter.</p></noscript><div class="catalog" data-catalog data-view="grid">${entries.map(card).join('')}</div><div class="empty" data-empty hidden><h2>No matching entries</h2><p>Try “ADB”, “interface”, or “notification”, or clear the filters.</p><button class="button js-only" type="button" data-reset>Clear filters</button></div><div class="notice"><p>Community plugins are open for listing. Existing release packages can be installed in DSHA with the <a href="/guide/#plugin">link install guide</a>; authors can <a href="/submit/">submit their work</a>. The catalog never treats a browser record as an on-device install.</p></div></section></div><section class="bottom-band"><div><h2>Have a useful plugin? Help more people use it.</h2><p>Provide a release package, requirements, and a test record. After review it can enter the catalog.</p></div><a class="button primary" href="/submit/">See submission requirements <span aria-hidden="true">→</span></a></section>`;
}
function detail(e) {
  const pack=e.download;
  const action=e.kind==='builtin'?`<span class="badge">Bundled with DSHA ${site.version}</span><h2>Manage in the app</h2><p class="muted small-text">No extra download is needed. Enable or disable it, then restart Web for the change to take effect.</p><a class="button primary" href="${escape(installLink(e))}">Open DSHA plugin management</a><a class="button" href="#usage">See enablement steps</a><a class="button" href="/download/">Download DSHA</a>`:e.kind==='plugin'?`<span class="badge">Community plugin · ${escape(e.version)}</span><h2>Install in DSHA</h2><p class="muted small-text">Open DSHA to review the actual plugin details, confirm install, then restart Web.</p><a class="button primary" href="${escape(installLink(e))}">Install in DSHA</a><label for="install-url">Backup install link</label><div class="copy-row"><input id="install-url" readonly value="${escape(pack.url)}"><button class="button primary js-only" type="button" data-copy-target="install-url">Copy install link</button></div>${external(pack.url,'Download the release package','button')}<a class="button soft" href="/guide/#plugin">Link install guide</a><p class="small-text muted">SHA-256 (checked by the app when launched from this page)</p><code class="hash">${pack.sha256}</code>`:`<span class="badge">DSHA rc1.1 adaptation</span><h2>Get the skill</h2><p class="muted small-text">Download it and place it in the skills directory. Do not install this file with Import plugin package.</p><a class="button primary" href="${pack.url}" download>Download skill package · ${(pack.bytes/1024).toFixed(1)} KB</a><a class="button" href="/downloads/skills/${e.id}/SKILL.md" download="${e.id}-SKILL.md">Download SKILL.md</a><a class="button soft" href="/guide/#skill">Skill install guide</a><p class="small-text muted">SHA-256</p><code class="hash" id="skill-hash">${pack.sha256}</code><a class="small-text" href="${pack.url}.sha256" download>Download checksum file</a>`;
  return `<nav class="breadcrumb" aria-label="Current location"><a href="/">Marketplace</a> / ${escape(e.name)}</nav><div class="detail-head"><span class="tile-icon ${e.kind}">${icon(e.icon)}</span><div><p class="eyebrow">${labels[e.kind]}</p><h1>${escape(e.name)}</h1><p class="package">${escape(e.packageName)} · ${escape(e.version)}</p></div></div><div class="detail-layout"><div class="detail-main"><section class="panel"><h2>What it does</h2><p>${escape(e.description)}</p><div class="notice"><p>${escape(e.example)}</p></div><div class="tags">${e.tags.map(t=>`<span class="tag">${escape(t)}</span>`).join('')}</div></section><section class="panel"><h2>Before you start</h2>${list(e.requirements)}</section><section class="panel" id="usage"><h2>${e.kind==='builtin'?'Enable and verify':'Install and verify'}</h2>${steps(e.steps)}<div class="actions"><a class="button" href="/guide/#${e.kind==='builtin'?'builtin':e.kind==='plugin'?'plugin':'skill'}">Full install guide</a></div></section><section class="panel"><h2>Permissions and data</h2>${list(e.permissions)}<p class="small-text muted">These notes help you judge the requirements. They are not a separate plugin sandbox.<a href="/security/">Learn DSHA permission boundaries</a></p></section><section class="panel"><h2>Compatibility notes and limits</h2><p>${escape(e.verification)}</p>${list(e.limitations)}<p class="small-text muted">Checked on: ${e.checkedAt || '2026-09-06'} · Checked with dsh ${e.testedDsh || '0.1.2-rc.1'}</p></section>${e.screenshot?`<section class="panel"><h2>On-device screenshot</h2><img class="plugin-screenshot" src="${e.screenshot.url}" width="1080" height="2400" loading="lazy" alt="${escape(e.screenshot.caption)}"><p class="screenshot-caption">${escape(e.screenshot.caption)}</p></section>`:''}<section class="panel"><h2>Source and feedback</h2><p>${external(e.source,'View the source project')} · ${external(site.repository+'/issues','Report a DSHA issue')}</p><p class="small-text muted">Maintained by: ${escape(e.author)} · ${escape(e.license)} license. ${e.kind==='skill'?'The skill adaptation keeps the project license. Treat the file downloaded from this page as the modified source.':'Follow the source project for the release package and license scope.'}</p></section></div><aside class="detail-aside" aria-label="Get it and version details"><section class="panel">${action}<button class="button js-only" type="button" data-share>Copy this page link</button><label class="sr-only" for="share-url">Shareable page link</label><input class="share-fallback" id="share-url" value="${site.origin}/plugins/${e.id}/" readonly hidden></section><section class="panel"><dl><div class="fact"><dt>Checked on DSHA</dt><dd>${e.testedDsha || '1.2.0-rc1.1'}</dd></div><div class="fact"><dt>Runtime</dt><dd>Linux arm64 · Ubuntu</dd></div><div class="fact"><dt>Type</dt><dd>${labels[e.kind]}</dd></div><div class="fact"><dt>Source</dt><dd>${escape(e.author)}</dd></div><div class="fact"><dt>License</dt><dd>${escape(e.license)}</dd></div></dl></section></aside></div>`;
}
const guide=`<div class="prose"><header class="page-head"><p class="eyebrow">INSTALL & USE</p><h1>Choose the right install path.</h1><p class="lede">For DSHA ${site.version}, with bundled dsh ${site.dsh}. Plugins, skills, and bundled features are managed separately.</p></header><nav class="article-index" aria-label="On this page"><a href="#plugin">Community plugins</a><a href="#skill">Agent Skill</a><a href="#builtin">Bundled plugins</a><a href="#faq">FAQ</a></nav><section class="panel" id="plugin"><h2>Install a community plugin from a link</h2><div class="actions"><a class="button primary" href="/install/">Open a plugin link in DSHA</a></div>${steps(['On the author page, pick a built, pinned release package. Prefer the HTTPS download link of a Release asset.','Launch DSHA from this site’s install page, or paste the link in the app marketplace so it can parse the actual package.','Confirm the plugin name, author, version, and compatibility, then wait for dependencies after you confirm install.','After a successful install, restart Web from Start, then return to Plugins to check status and verify the feature.'])}<p>GitHub repositories, branches/subdirectories, Release assets, and ZIP, TAR, TAR.GZ, or TGZ archives are supported. Keep only one link when copying. A plain source repository may not contain an installable artifact.</p><h3>When the network is restricted</h3><p>Download the plugin package first, then choose Import plugin package in the DSHA marketplace and pick the file with the system picker. Installed plugins can be exported for backup or import on another DSHA.</p><h3>Plugins published on npm</h3><p>Use this format in the DSHA terminal, replacing author, plugin, and version with the real published values:</p>${code('dsha-plugin install @author/plugin@version')}<p>A plain npm install does not add it to the Web profile. After install, detect the plugin in the app, confirm source and dependencies, then enable it if needed. The installer does not run prepare, build, or install scripts, so the package must already be built.</p></section><section class="panel" id="skill"><h2>Add an Agent Skill</h2><p>A skill is a directory that contains SKILL.md. Download and unpack the skill package from this site, then place it here through a file manager DSHA can access:</p>${code('/root/.agents/skills/skill-name/SKILL.md','skill-path')}<p>For example, the phone command skill lives at /root/.agents/skills/device-shell/SKILL.md. Project-specific skills can also go in .agents/skills/ under the actual project root.</p>${steps(['Keep the skill-name/SKILL.md directory layout. Avoid nested archive folders.','Use a skill-capable Agent preset such as standard. Custom or minimal presets may have no skill provider.','The directory usually updates automatically. Start a new session and ask the agent to use the skill for a read-only check. If it is not found, check the path, file format, and preset.'])}<p>Skill packages cannot be installed with Import plugin package. Do not treat a path inside the phone as a Windows file path.</p></section><section class="panel" id="builtin"><h2>Manage bundled plugins</h2><p>This version includes bundled device-operation guidance, task-complete notifications, a live overlay, and a mobile interface. Checked on DSHA on a detail page is the version actually tested for that entry.</p>${steps(['Open DSHA → Plugins and find the plugin by name.','Use the switch to enable or disable it. System notifications, overlay, ADB, and similar grants are separate.','Restart Web from Start, then verify the matching feature.'])}<p>Bundled plugins are managed with switches. Official core pieces update with the dsh environment. This website does not read plugin status or the bridge token from your phone.</p></section><section id="faq"><h2>FAQ</h2>${[['Why is there no one-tap install success on the website?','The site can launch DSHA rc1.3 and later. On a computer you can continue by scanning a QR code. The app reads the actual plugin package, checks compatibility, and waits for your confirmation. The result shown in the app is authoritative.'],['It says dsh.bundle.patch is missing.','This package does not meet the dsh plugin contract. Confirm you downloaded the author’s built plugin package, not a plain npm library, source snapshot, or Skill.'],['It is installed. Why nothing changed?','Restart Web after installing, enabling, or disabling a dsh plugin. A successful registration still needs a real load check. Skill directory scanning is a separate mechanism.'],['How do I choose standard vs compatibility?','Prefer standard on Android 11+. Use compatibility on Android 6–10. Both builds are arm64 only. Device channels and browser capabilities still differ on older systems.'],['How should I report a plugin error?','Record the DSHA version, standard or compatibility edition, plugin version, source link, and the full error. Hide API keys, bridge tokens, and private chat before sending.'],['What happens if I import the same name?','The current installer updates the plugin entity and keeps the previous disabled state. Export first before important changes. A sudden power loss or force-stop is not a full transaction.']].map(([q,a])=>`<details class="faq"><summary>${q}</summary><p>${a}</p></details>`).join('')}</section><div class="actions"><a class="button primary" href="/">Back to marketplace</a><a class="button" href="/download/">Download ${site.version}</a></div></div>`;
const security=`<div class="prose"><header class="page-head"><p class="eyebrow">PERMISSIONS & DATA</p><h1>Know where capabilities live, and where data goes.</h1><p class="lede">Based on how DSHA ${site.version} currently runs. Review the related conditions before you install.</p></header><section class="panel"><h2>Website and phone environment</h2><p>This website provides a catalog, docs, and downloads. Theme and list preferences stay in the current browser. The site does not connect to the phone’s 3090 local bridge, request an API key or bridge token, or read plugins installed on the phone.</p><p>When you visit pages or download files, the server may keep ordinary access logs. Submissions go through a GitHub page you open yourself and follow GitHub’s public visibility and privacy rules.</p></section><section class="panel"><h2>Plugin runtime boundary</h2><p>dsh plugins run in DSHA’s Ubuntu container. Archive path and entry checks can reject malformed packages. That is not a code review, and it is not a per-plugin kernel sandbox.</p><p>“No extra Android permission” means it does not request additional system permissions. It does not guarantee the plugin code cannot reach host-accessible data. Prefer packages with source, a pinned version, and a clear maintainer.</p></section><section class="panel"><h2>System grants and external services</h2>${list(['ADB or Shizuku: can run device commands. Grants and channel state are managed in Android and DSHA.','Overlay: task text may appear over other apps and can be visible during screen sharing.','System notifications: affect whether task notifications can appear.','Vision models: screenshots are sent to the model service you configured. They may include personal content on screen and can incur charges.','File access: decides whether shared directories can be read or written. Without enough grants, some data stays in the app-private directory.'])}</section><section class="panel"><h2>Backups, deletion, and credentials</h2><p>DSHA supports public-directory migration and backup restore. Whether uninstall keeps data depends on the real storage location, file grants, and backups. Do not assume everything is kept automatically.</p><p>Deleting a third-party plugin removes its install and registration. Export first if you need to keep it. API keys are encrypted with Android Keystore. The device-bridge token is a local credential and should not go into public backups, log screenshots, or submissions.</p></section><section class="panel"><h2>How to read catalog verification notes</h2><p>This round checked the dsh and bundled plugin versions inside both rc1.1 APKs. Entries distinguish in-package checks from actual device runs. Author claims or package-format checks are not treated as all-device compatibility.</p><p>Skills on this site are rc1.1 adaptation documents. Results depend on the agent, device grants, and external services. If something is canceled, denied, or times out on confirmation, stop and check why.</p></section><p>${external(site.repository+'/issues','Report an issue')} · QQ group ${site.group}</p></div>`;
function downloadPage(releases) {
  return `<header class="page-head"><p class="eyebrow">DOWNLOAD DSHA</p><h1>Run DeepSeek Harness on Android.</h1><p class="lede">${site.version} ${channelLabel} · Bundled Ubuntu 24.04, Node.js 24, and dsh ${site.dsh}. Both builds are arm64-v8a only.</p></header><div class="downloads">${releases.map(r=>`<section class="download-card ${r.flavor==='standard'?'recommended':''}"><span class="badge">${r.flavor==='standard'?'Preferred on Android 11+':'Older-system compatible'}</span><h2>${r.flavor==='standard'?'Standard':'Compatibility'}</h2><p class="muted">${r.flavor==='standard'?'Android 11+ · system WebView':'Android 6+ · WebView / bundled Gecko'}</p>${list(r.flavor==='standard'?['Uses system WebView. Keep the browser component updated.','For arm64 phones on Android 11 and later.']:['Use this build on Android 6–10. Newer systems can also install it.','Older WebViews can use bundled Gecko, which increases package size.'])}<div class="actions"><a class="button primary" href="${r.url}" download>Download APK · ${r.MiB} MiB</a></div><p class="small-text">${r.filename}<br>${r.bytes.toLocaleString('en-US')} bytes · versionCode ${site.versionCode}</p><small>SHA-256</small><code class="hash" id="hash-${r.flavor}">${r.sha256}</code><div class="actions"><a href="${r.url}.sha256" download>Download checksum file</a><button class="button small js-only" type="button" data-copy-target="hash-${r.flavor}">Copy digest</button></div></section>`).join('')}</div><section class="panel" id="release-notes"><h2>This ${channelLabel} update</h2>${list(current.notes.split(/\n+/).filter(Boolean))}<div class="actions">${external(releasePage,'GitHub '+channelLabel+' and full comparison','button')}</div></section><div class="notice"><p>${site.version} is ${channelLabel}. ${current.channel==='stable'?'Both the app stable channel and the preview channel can discover this official update by version code.':'Switch the app to the preview channel to check this preview update.'} The same Ubuntu base updates only the managed runtime. A base-environment change protects data first, then rebuilds. The minimum OS requirement means it can install, not that every OS version and device capability was device-tested.</p></div><section class="page-head"><h2>First run</h2><div class="step-grid">${[['01','Install the APK','Pick the package for your Android version. Back up before overlaying. If you need to uninstall, confirm you have a recoverable backup.'],['02','Extract the environment','The first launch extracts bundled Ubuntu. This takes time and free space. Keep the page open until it finishes.'],['03','Configure and start','Set the model service and API key on the config page, then start Web. Model calls need a working service configuration.'],['04','Add capabilities','Start or stop bundled plugins on the management page. Install community plugins from a link. Place Agent Skills in the skills directory.']].map(([n,t,d])=>`<article class="step-box"><span class="step-number">${n}</span><h3>${t}</h3><p>${d}</p></article>`).join('')}</div></section><section class="bottom-band"><div><h2>Having install or startup trouble?</h2><p>Check the install guide and the full error first. Include version and system details when you report it.</p></div><a class="button" href="/guide/#faq">See the FAQ</a></section>`;
}
const about=`<div class="prose"><header class="page-head"><p class="eyebrow">ABOUT DSHA</p><h1>Bring a complete agent environment onto the phone.</h1><p class="lede">DSHA is the Android launcher for DeepSeek Harness. No root and no Termux. The APK deploys an Ubuntu environment, then you work with the embedded web UI and the agent.</p></header><section class="panel"><h2>Current ${channelLabel} ${site.version}</h2><p>Bundled dsh ${site.dsh}, Node.js 24, and Ubuntu 24.04 arm64. Standard uses system WebView. Compatibility adds Gecko and a native runtime suited to older Android.</p><p>DSHA handles environment install, startup, plugin import/export, device channels, and the Android UI. DeepSeek Harness provides the core agent. Third-party plugins and skills list their own sources.</p></section><section class="panel"><h2>Already available</h2>${list(['Bundled offline environment, Web start, and recovery entry points.','Link install, local import, plugin management, and multi-select export.','PTY terminals, plus npm, Python, and Git in the current environment.','ADB, Shizuku, notifications, and the overlay, subject to grants.','Manual backup/restore, personal-directory protection, and rollback after a failed update.'])}</section><section class="panel"><h2>Standard and compatibility</h2><div class="table-wrap"><table class="article-table"><thead><tr><th>Item</th><th>Standard</th><th>Compatibility</th></tr></thead><tbody><tr><td>Minimum Android</td><td>11 / API 30</td><td>6 / API 23</td></tr><tr><td>Architecture</td><td>arm64-v8a</td><td>arm64-v8a</td></tr><tr><td>Web engine</td><td>System WebView</td><td>WebView / Gecko</td></tr><tr><td>Ubuntu and feature code</td><td colspan="2">Shared. System grants and device capabilities still differ by Android version</td></tr></tbody></table></div></section><section class="panel"><h2>Contribute</h2><p>This release is maintained and published by contributor <a href="https://github.com/ym2025szz">@ym2025szz</a>. Thanks to original author <a href="https://github.com/qiannianhuanxiang">@qiannianhuanxiang</a> and the other contributors.</p><p>To submit a plugin, start with the author guide on this site. For Android app changes, see the repository README and AGENTS.md. Issues and plugin discussion: QQ group ${site.group}.</p><div class="actions"><a class="button primary" href="/submit/">Submit a plugin</a>${external(site.repository,'Open the project','button')}</div></section><div class="actions"><a class="button primary" href="/download/">Download DSHA</a><a class="button" href="/">Browse the marketplace</a></div></div>`;
const submit=`<div class="prose"><header class="page-head"><p class="eyebrow">FOR AUTHORS</p><h1>Submit your plugin or skill.</h1><p class="lede">Start with something useful and reproducible. Provide a clear source, requirements, and test results so others can use it on a phone.</p></header><section class="panel"><h2>Prepare the release first</h2><h3>dsh plugin</h3>${list(['Provide a valid npm package name, version, and license, and declare dsh.bundle.patch in package.json.','Declared patch, main, or cordis.entry files must be inside the release package.','Provide a built Release asset or npm package. The installer does not run prepare/build/install scripts.','Native dependencies must target Linux/glibc arm64. Do not ship Windows-only or x64-only artifacts.','Record the DSHA build, dsh version, install method, and load verification.','Archives must stay under 256 MiB compressed and 768 MiB / 50,000 files unpacked. Out-of-tree paths and outside symlinks are rejected.'])}<h3>Agent Skill</h3>${list(['Organize as skill-name/SKILL.md. Frontmatter must include name and description.','Write trigger conditions, prerequisites, steps, verification, and failure handling.','Device commands must use the rc1.1 app wrapper entry. Do not include historical Termux paths or settings that bypass grants.','Say whether screenshots or files go to an external service. Do not submit API keys, tokens, or real private data.'])}<p>Review checks package format, documentation, and test records separately. Listing does not mean an independent sandbox or all-device compatibility.</p></section><section class="panel"><h2>Generate the submission</h2><p>Preview first, then open a GitHub issue yourself. The form is not saved on this site’s server.</p><noscript><p>You can open ${external(site.repository+'/issues/new','the GitHub submission page')} directly and fill in the source, release package, and test record as required above.</p></noscript><p>${external(site.repository+'/issues/new','Go to GitHub to submit')}</p><form data-submit-form class="js-only submission-form"><div class="form-grid">${[['name','Name','text',80],['source','Source link (HTTPS)','url',300],['version','Release version / skill revision date','text',60],['artifact','Release package link (optional, HTTPS)','url',300],['license','License','text',80]].map(([name,label,type,max])=>`<div class="form-field"><label for="submit-${name}">${label}</label><input id="submit-${name}" name="${name}" type="${type}" maxlength="${max}"${type==='url'?' pattern="https://.*"':''}${name==='artifact'?'':' required'}></div>`).join('')}<div class="form-field"><label for="submit-kind">Type</label><select id="submit-kind" name="kind"><option value="dsh plugin">dsh plugin</option><option value="Agent Skill">Agent Skill</option></select></div>${[['description','What it does'],['permissions','Dependencies, data, and permissions'],['tested','DSHA version and test results']].map(([name,label])=>`<div class="form-field full"><label for="submit-${name}">${label}</label><textarea id="submit-${name}" name="${name}" maxlength="800" required></textarea></div>`).join('')}</div><p class="form-note">GitHub issues are usually public. Do not include credentials, private chat, or device identifiers.</p><button class="button primary" type="submit">Generate and preview submission</button></form><section id="submission-result" data-submit-result tabindex="-1" hidden><h3>Review the submission</h3><pre class="submission-preview" id="submission-preview" data-submission-preview></pre><div class="actions"><a class="button primary" data-submit-link href="${site.repository}/issues/new" target="_blank" rel="noopener noreferrer">Open GitHub to submit</a><button class="button js-only" type="button" data-copy-target="submission-preview">Copy submission</button></div><p class="form-note">This generates a draft. Maintainers receive it only after you click submit on GitHub.</p></section></section></div>`;

// dist holds only the current build. Historical delivery files stay in the workspace release directory.
if(path.resolve(output)!==path.resolve(project,'dist') || path.dirname(output)!==project)throw new Error('Build directory is out of bounds');
if(fs.existsSync(output)){if(fs.lstatSync(output).isSymbolicLink())throw new Error('Build directory cannot be a symlink');fs.rmSync(output,{recursive:true});}
fs.mkdirSync(output,{recursive:true});
for (const [name,ext] of [['styles','css'],['theme','js'],['app','js']]) {
  const value=(name==='app'?read('node_modules/qrcode-generator/dist/qrcode.js')+'\n':'')+read(`src/${name}.${ext}`)+(name==='app'?'\n'+read('src/install.js'):''), filename=`${name}.${sha(value).slice(0,12)}.${ext}`;
  write(`assets/${filename}`,value);if(name==='styles')css=filename;else if(name==='theme')theme=filename;else app=filename;
}
write('assets/qrcode-LICENSE.txt',read('src/qrcode-LICENSE.txt'));
const logoPng=fs.readFileSync(path.join(repository,'app/src/main/res/mipmap-xxxhdpi/ic_launcher.png'));
const faviconPng=fs.readFileSync(path.join(repository,'app/src/main/res/mipmap-mdpi/ic_launcher.png'));
logoHash=sha(Buffer.concat([logoPng,faviconPng])).slice(0,12);
logoFile=`dsha-logo.${sha(logoPng).slice(0,12)}.png`;
faviconFile=`dsha-favicon.${sha(faviconPng).slice(0,12)}.png`;
write(`assets/${logoFile}`,logoPng);write(`assets/${faviconFile}`,faviconPng);
write('favicon.png',faviconPng);write('apple-touch-icon.png',logoPng);
// Keep the old favicon.svg URL, but reuse the APK artwork without redrawing or recoloring.
write('favicon.svg',`<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 192 192"><image width="192" height="192" href="data:image/png;base64,${logoPng.toString('base64')}"/></svg>`);
const iconImages=[faviconPng,logoPng],icoHeader=Buffer.alloc(6+16*iconImages.length);
icoHeader.writeUInt16LE(1,2);icoHeader.writeUInt16LE(iconImages.length,4);
let imageOffset=icoHeader.length;
iconImages.forEach((png,i)=>{const p=6+i*16;icoHeader[p]=png.readUInt32BE(16)%256;icoHeader[p+1]=png.readUInt32BE(20)%256;icoHeader.writeUInt16LE(1,p+4);icoHeader.writeUInt16LE(32,p+6);icoHeader.writeUInt32LE(png.length,p+8);icoHeader.writeUInt32LE(imageOffset,p+12);imageOffset+=png.length;});
write('favicon.ico',Buffer.concat([icoHeader,...iconImages]));
const license=fs.readFileSync(path.join(repository,'LICENSE'),'utf8');
for(const e of entries.filter(e=>e.screenshot)){
  if(!/^[a-z0-9-]+\.png$/.test(e.screenshot.file))throw new Error('Screenshot filename is invalid');
  const bytes=fs.readFileSync(path.join(project,'src/community',e.screenshot.file));
  const url=`/assets/${e.id}.${sha(bytes).slice(0,12)}.png`;
  write(url.slice(1),bytes);e.screenshot.url=url;
}
for(const e of entries.filter(e=>e.kind==='skill')){
  const dir=`downloads/skills/${e.id}`;
  write(`${dir}/SKILL.md`,read(`src/skills/${e.id}/SKILL.md`));write(`${dir}/LICENSE`,license);
  write(`${dir}/README.txt`,`DSHA ${e.testedDsha || '1.2.0-rc1.1'} historical skill adaptation\n\nSkill compatibility for the current official release has not been rechecked item by item. Follow the test scope on the catalog page.\nPlace ${e.id}/SKILL.md at /root/.agents/skills/${e.id}/SKILL.md.\nUse a skill-capable preset such as standard.\nThis is not a dsh bundle and cannot be installed with Import plugin package.\nDocs: https://dsha.cc/guide/#skill\nSource: ${e.source}\n`);
  const filename=`${e.id}-dsha-rc1.1.tar.gz`,target=path.join(output,'downloads/skills',filename);
  execFileSync('tar',['-czf',target,'-C',path.join(output,'downloads/skills'),e.id],{windowsHide:true});
  const hash=await digestFile(target);write(`downloads/skills/${filename}.sha256`,`${hash}  ${filename}\n`);
  e.download={url:`/downloads/skills/${filename}`,bytes:fs.statSync(target).size,sha256:hash,format:'tar.gz'};
}
const releases=[];
for(const r of current.artifacts){
  const source=path.join(repository,'release',r.filename);
  if(await digestFile(source)!==r.sha256)throw new Error(`Release file changed: ${r.filename}`);
  const address=new URL(r.url);if(address.origin!==site.origin || address.pathname!==`/downloads/${site.version}/${r.filename}`)throw new Error('APK URL does not match the official site');
  const url=address.pathname;copy(source,url.slice(1));write(url.slice(1)+'.sha256',`${r.sha256}  ${r.filename}\n`);
  releases.push({...r,MiB:(r.bytes/1048576).toFixed(2),url,minAndroid:r.flavor==='standard'?11:6,architecture:r.abi});
}
write('index.html',page({title:'Marketplace',description:`Discover DSHA bundled plugins and Agent Skills. Review requirements, download skills, and get DSHA ${site.version}.`,route:'/',body:market()}));
write('marketplace.html',page({title:'Marketplace',description:'DSHA plugin and skill catalog.',route:'/',body:market(),noindex:true}));
for(const e of entries)write(`plugins/${e.id}/index.html`,page({title:e.name,description:e.summary,route:`/plugins/${e.id}/`,body:detail(e)}));
for(const [route,title,active,body,description] of [['guide','Install guide','guide',guide,`Install plugins from a link, import or export packages, add skills, and manage bundled plugins in DSHA ${site.version}.`],['security','Permissions and data','',security,'Learn the data and permission boundaries for DSHA plugins, skills, device channels, and external models.'],['download','Download DSHA','download',downloadPage(releases),`Download DSHA ${site.version} standard and compatibility APKs, with SHA-256 and install notes.`],['about','About DSHA','about',about,'DSHA brings DeepSeek Harness and an Ubuntu environment to Android.'],['submit','Submit a plugin','submit',submit,'See publishing requirements for dsh plugins and Agent Skills, then generate a marketplace listing request.']])write(`${route}/index.html`,page({title,description,active,route:`/${route}/`,body}));
write('404.html',page({title:'Page not found',description:'This page does not exist. Return to the DSHA marketplace.',route:'/404.html',active:'',noindex:true,body:'<section class="page-head"><p class="eyebrow">404</p><h1>This page was not found.</h1><p class="lede">The link may have changed. Search the catalog or open the install guide.</p><div class="actions"><a class="button primary" href="/">Back to marketplace</a><a class="button" href="/guide/">Install guide</a></div></section>'}));
const publicEntries=entries.map(e=>({...e,detailUrl:`${site.origin}/plugins/${e.id}/`,compatibleDsha:e.testedDsha || '1.2.0-rc1.1',dsh:e.testedDsh || '0.1.2-rc.1',checkedAt:e.checkedAt || '2026-09-06'}));
write('api/catalog.json',JSON.stringify({schemaVersion:1,updatedAt:site.checkedAt,dsha:site.version,dsh:site.dsh,entries:publicEntries},null,2)+'\n');
write('api/releases.json',JSON.stringify({schemaVersion:1,version:site.version,versionCode:site.versionCode,channel:current.channel,dsh:site.dsh,releases},null,2)+'\n');
write('api/updates.json',JSON.stringify(manifest,null,2)+'\n');
write('.well-known/assetlinks.json',JSON.stringify([{relation:['delegate_permission/common.handle_all_urls'],target:{namespace:'android_app',package_name:manifest.packageName,sha256_cert_fingerprints:[manifest.certificateSha256.match(/../g).join(':').toUpperCase()]}}],null,2)+'\n');
write('install/index.html',page({title:'Install a plugin',description:'Review plugin details in DSHA and confirm install, or download DSHA.',route:'/install/',active:'market',body:installPage}));
write('robots.txt',`User-agent: *\nAllow: /\nSitemap: ${site.origin}/sitemap.xml\n`);
write('sitemap.xml',`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${[...new Set(routes)].map(route=>`<url><loc>${site.origin}${route}</loc><lastmod>${site.checkedAt}</lastmod></url>`).join('')}</urlset>`);
write('health.json',JSON.stringify({site:'dsha.cc',version:site.version,catalogCount:entries.length,buildId:sha(css+app+theme+logoHash+JSON.stringify(publicEntries)+JSON.stringify(manifest)+read('scripts/build.mjs')+read('scripts/install-page.mjs')).slice(0,16)})+'\n');
write('LICENSE',license);
const files=[];
function walk(dir){for(const item of fs.readdirSync(dir,{withFileTypes:true})){const full=path.join(dir,item.name);if(item.isDirectory())walk(full);else if(item.name!=='checksums.sha256')files.push(full);}}
walk(output);const sums=[];for(const f of files.sort())sums.push(`${await digestFile(f)}  ${path.relative(output,f).replaceAll('\\','/')}`);write('checksums.sha256',sums.join('\n')+'\n');
console.log(`Built ${routes.length} pages, ${entries.length} entries and ${releases.length} verified APK downloads in ${output}`);
